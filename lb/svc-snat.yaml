apiVersion: v1
kind: Service
metadata:
  name: test-service-snat
  annotations:
    service.cilium.io/loadbalancer-mode: "snat"
  labels:
    gateway: blue
spec:
  type: LoadBalancer
  ports:
  - port: 80
    targetPort: 80
  selector:
    app: test-app