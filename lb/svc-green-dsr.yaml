apiVersion: v1
kind: Service
metadata:
  name: test-service-dsr
  annotations:
    service.cilium.io/loadbalancer-mode: "dsr"
  labels:
    gateway: green
spec:
  type: LoadBalancer
  ports:
  - port: 80
    targetPort: 80
  selector:
    app: nginx-green